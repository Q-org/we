const x = 100;
function greet(name: string, arg1? = 2, arg2? = 3): string {
  // more statement
  //console.log(name)
  function fn1() {
    xxx函数(...arguments);
  }
  () => {};
  return "Hello, " + name;
  method(a1, a2, a3);
  {
    if (true) {
      return Hast._htmr(...arguments);
    }
    return someFunc(...argrements);
  }
}
class ExampleClass {
  static method(a1, a2, a3) {
    if (true) {
      return Hast._htmr(...arguments);
    }
    return someFunc(...argrements);
  }
}
